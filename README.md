# IONS Genesis

Experiment: Can CBB traversal produce answers that rival or outperform raw LLM answers?

## Components
- backend/ — Python FastAPI
- frontend/ — Next.js (coming in a later step)
- docker-compose.yml — Postgres + API